- Adding proper logging functionality
    - for now just add messages
    - later figure out a way to handle verbose globally in reusable manner
        - promises?
        - global config file?
        - pass param to services?
        ...

- handle function inputs and CLI nicely. (not provided, etc.)
    - use commander itself
        ommander proper handling of cli options and inputs

- typescriptfy the project

- deploy to vps for @h1reports
    - pm2?
    - crontab?
    - systemcdl?
    - forever?